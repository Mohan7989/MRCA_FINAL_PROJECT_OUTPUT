# NOTE: H2 dev profile removed. Use src/main/resources/application.properties (MySQL) for configuration.
# If you need a separate profile, recreate this file with your desired settings.
# === PostgreSQL Configuration for Render ===
spring.datasource.url=jdbc:postgresql://dpg-d3obalqli9vc73c0frb0-a/student_resources_db
spring.datasource.username=student_resources_db_user
spring.datasource.password=gxi7FwBBMtlpPzlezz35gCzWdVbhvnlY
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server port (Render uses PORT env)
server.port=${PORT:8080}


# File upload directory (keeps same local path)
file.upload-dir=src/main/resources/static/uploads
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
