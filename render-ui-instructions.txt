Render Web Service - exact values to enter (copy/paste)

1) Repository
   - Git provider: GitHub
   - Repository: Mohan7989/MRCA_FINAL_PROJECT_OUTPUT   (or your repo path)

2) Create -> New Web Service
   - Name: student-resources-backend
   - Environment: Docker
   - Branch: main
   - Root Directory: backend        <-- exact, no leading/trailing spaces
   - Dockerfile Path: Dockerfile   <-- exact, case-sensitive
   - Plan: free (or your chosen plan)
   - Region: (choose nearest)

3) Port & Health
   - Port: 8080
   - Health check path (optional): /api/health
   - Health check protocol: HTTP

4) Environment variables (add these in the Service → Environment → Environment Variables)
   - SPRING_DATASOURCE_URL = jdbc:mysql://<MYSQL_HOST>:3306/student_resources?useSSL=false&serverTimezone=UTC
   - SPRING_DATASOURCE_USERNAME = mohan
   - SPRING_DATASOURCE_PASSWORD = <your_db_password_here>
   - SPRING_DATASOURCE_DRIVER = com.mysql.cj.jdbc.Driver
   - SPRING_JPA_HBM2DDL = update
   - FILE_UPLOAD_DIR = /data/uploads
   - JAVA_OPTS = -Xmx512m

5) Notes / Troubleshooting
   - Make sure the file backend/Dockerfile exists in the repo root exactly at that path.
   - Do NOT put "backend/Dockerfile" into the Root Directory field — Root Directory must be a directory (backend).
   - If using render.yaml, Render expects the same values; verify the file in repo root is the one we committed.
   - If build fails, open Render Logs → check for "Cannot find Dockerfile" or database errors.
